/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "assets.nflxext.com",
      "static.licdn.com",
      "th.bing.com",
      "www.themoviedb.org",
    ],
  },
};

module.exports = nextConfig;
